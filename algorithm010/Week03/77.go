package main

import "fmt"

func main() {
	var n, k int
	fmt.Println(combine(n, k))
}

func combine(n, k int) [][]int{
	// temp:=make([]int,k)
	res:=[][k]int{}
	for i:=0;i<n;i++{
		temp:=[k]int{}
		res=apped()
	}
}
